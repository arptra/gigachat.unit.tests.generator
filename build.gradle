subprojects {
    apply plugin: 'java'

    group = 'com.gigachat.unit.tests.generator'
    version = '0.1.0'

    repositories {
        mavenCentral()
        maven { url 'https://repo.gradle.org/gradle/libs-releases' }
    }

    java {
        toolchain {
            languageVersion = JavaLanguageVersion.of(21)
        }
    }

    tasks.withType(Test).configureEach {
        useJUnitPlatform()
    }
}

if (project.findProject(':plugin') != null) {
    tasks.register('diffGenUnitTest') {
        group = 'verification'
        description = 'Runs diff-based unit test generation from the plugin module.'
        dependsOn(':plugin:diffGenUnitTest')
    }

    tasks.register('genAiTest') {
        group = 'verification'
        description = 'Runs scan-mode AI unit test generation from the plugin module.'
        dependsOn(':plugin:genAiTest')
    }
}
